#!/bin/sh
# Shared aliases — sourced by both .bashrc and .zshrc

alias g='git'
alias vim='nvim'
alias gst='git status'
alias gcm='git commit -m'
alias dotfiles='cd ~/dotfiles && vim'
alias ll='ls -la'
alias ..='cd ..'
