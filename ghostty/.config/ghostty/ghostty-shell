#!/bin/sh
# Ghostty `command`. Quick terminal (cmd+`) -> plain shell as a utility terminal;
# every other window/tab -> tmux session "main" (attach or create).
if [ -n "$GHOSTTY_QUICK_TERMINAL" ]; then
    exec /bin/zsh -l
fi
exec /opt/homebrew/bin/tmux new-session -A -s main
